#include<iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(i=0;i<n;i++)
        cin>>arr[i];
    for(i=0;i<n-1;i++)
    {   k=i+2;
        for(j=i+1;j<n;j++)
        {

            if(arr[i]+arr[j]==arr[k++]
            else
            {
                if(arr[i]+arr[j]==arr[k])
            }
        }
    }
}
